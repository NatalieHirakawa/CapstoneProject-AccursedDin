using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Auth: Chris W.

public class VLLerp : VirtualListener {

    //[SerializeField] private float baseLevel = 0;
    //[SerializeField] private float maxLevel = 20;
    //protected Vector3 LerpPos;

    //private Vector3 TranslatedLerp(float valA, float valB, Vector3 pointA, Vector3 pointB)
    //{
        //return null;
    //}

    protected override void OnUpdate(float audioVal)
    {
        //audioVal = AudioPeer.m_audioSpectrum[index] * AudioPeer.multiplier;
        //Vector3 idealPosition = TranslatedLerp(baseLevel, maxLevel, transform.position, Null);
    }

}
